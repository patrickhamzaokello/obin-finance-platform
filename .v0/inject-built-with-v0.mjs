#!/usr/bin/env node
// v0 build hook - this script is called before next build
// It's used to inject v0-specific metadata into the build
// For now, this is a no-op that just exits successfully

process.exit(0);
